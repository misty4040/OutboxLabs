[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.build]
cmds = ["npm --workspace=backend run build"]

[start]
cmd = "cd backend && npx prisma db push --accept-data-loss && cd .. && npm --workspace=backend run start"
